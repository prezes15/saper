Description

This project is a simple console version of the classic Minesweeper game written in C++.
The player explores a 10x10 board while trying to avoid hidden mines. The game allows the user to reveal fields, place flags, and win by uncovering all safe fields.

The project uses dynamic memory allocation, recursion, pointer operations, and modular programming with header files.